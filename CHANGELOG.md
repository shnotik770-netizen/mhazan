# יומן שינויים

תיעוד של כל שינוי משמעותי במערכת, לפי תאריך. הרשומה החדשה ביותר למעלה.

---

## 2026-08-25

### חדש: תזרים חודשי בדוח מחלקה כולל צפי המשך תשלומי אשראי
- כשתורם משלם באשראי בתשלומים (למשל "7 מתוך 12"), התזרים החודשי בדוח מחלקה כולל עכשיו צפי אוטומטי להמשך התשלומים שנותרו — לפי הקצב הידוע (אותו סכום, כל חודש), עד סוף התוכנית.
- מניעת כפל ספירה: לכל תורם+מספר תשלומים+קטגוריה נלקחת רק ההופעה העדכנית ביותר (למשל אם יש גם רשומה מחודש 7 וגם מחודש 8 לאותו חיוב, נלקחת רק זו של חודש 8) — כדי שאותה תוכנית תשלומים לא תיספר פעמיים בצפי.
- זה חל **רק על תזרים חודשי בדוח מחלקה** (לא על תחזית הבנק הכללית ב-/forecast, ולא על "הכנסות עתידיות צפויות" הידניות שם).

### שיפורי דוח מחלקה (הדפסה + תצוגה)
- "תנועות עתידיות ידועות" הועבר לסוף הדוח (אחרי "תנועות עד היום" ותזרים חודשי) — הן פחות "סופיות" מההיסטוריה בפועל, ועכשיו קוראות כנספח בסוף ולא מתחרות איתה על המקום הראשון.
- שלושת הריבועים העליונים (סה"כ הכנסות / הוצאות / נטו) יישארו תמיד בשורה אחת גם בהדפסה.
- "תזרים חודשי מלא" כבר לא מתקפל/נסגר (בוטל החץ) — מוצג פתוח תמיד.
- כפתור "סמן כישנה" (ליד כל שורת הכנסה/הוצאה, למנהל בלבד) הפך לתפריט שלוש-נקודות (⋮) עם הפעולה בתוכו, כמו בשאר טבלאות המערכת — פחות טקסט צפוף בטבלה, ומקום מוכן לפעולות נוספות בעתיד.
- עמודת "סוג" בטבלת התנועות פוצלה לשתיים: "הכנסה / הוצאה" (כיוון התנועה) ו"סוג" (אמצעי תשלום/סוג בפועל: אשראי, מזומן, הו"ק, העברה, צ'ק, ידני, עמלת אשראי וכו'). קטגוריית הכנסה (למי שהייתה) מוצגת עכשיו כתגית ליד שם התורם בעמודת התיאור, במקום בתוך עמודת הסוג.

### נמצא ותוקן: הסיבה האמיתית שרישומים ידניים לא הופיעו בדוח מחלקה
- הודות ליומן הביקורת ולרישום השגיאות שנוספו הרגע — נמצאה הסיבה האמיתית לכך שרישומים ידניים (הכנסה/הוצאה ידנית) לא הופיעו **באף דוח מחלקה, אף פעם**: לטבלאות `bank_accounts` ו-`departments` יש שני קשרים (FK) ביניהן במסד הנתונים (חשבון שייך למחלקה + מחלקה עם "חשבון בית"), ושאילתת דוח המחלקה ניסתה לשלב מידע משתיהן בלי לציין לאיזה קשר להתייחס — Supabase דחה את השאילתה כשגויה ("יותר מקשר אחד אפשרי") בכל פעם, וכל הרישומים הידניים נעלמו בשקט מהדוח (בלי שגיאה גלויה למשתמש).
- זו בדיוק הסיבה שהדוגמאות שהבאת (חב"ד לנוער, דוברי צרפתית, חב"ד בעליה) הראו "אין תנועה" למרות שהנתונים היו תקינים לגמרי במסד הנתונים — תוקן עכשיו: השאילתה מציינת במפורש איזה משני הקשרים להשתמש בו.
- זה גם מה שגרם ל"התחשבנות פנימית בין מחלקות" (מטריצת יתרות) להיראות תקינה בזמן שדוח המחלקה הספציפי נראה חסר — שני המסכים האלה שולפים נתונים בדרכים שונות, ורק אחת מהן נפגעה.

### חדש: יומן ביקורת כללי לכל פעולה במערכת
- בהמשך לתקלה עם צ׳ק #1352266 (שם נוצר תיעוד רק לשינויי סטטוס צ׳ק) — נוסף עכשיו יומן ביקורת גורף שמתעד **כל** יצירה/עדכון/מחיקה בכל הטבלאות הפיננסיות והרשאות במערכת: חשבונות בנק, תנועות בנק, קטגוריות, פיצולי צ׳קים, צ׳קים והעברות, עמלת אשראי, מחלקות, הכנסות צפויות, הכנסות, יתרות בין-מחלקתיות, רישומים ידניים, פריסות תשלום, הוראות קבע ופיצוליהן, ספקים, הרשאות גישה למחלקות, ומשתמשים. לכל שינוי נשמר מי ביצע אותו, מתי, ומה בדיוק היה לפני ואחרי.
- מסך חדש "יומן ביקורת" (למנהלי כספים בלבד, בהגדרות מערכת ← "פתיחת יומן הביקורת") מציג את הרשומות עם סינון לפי טבלה / סוג פעולה / משתמש / טווח תאריכים, ופתיחת "פרטים" לכל שורה מציגה את הנתונים המלאים לפני/אחרי.
- ברמת מסד הנתונים: טבלת `audit_log` חדשה + טריגר גנרי אחד (`fn_audit_row_change`, מוגן — לא ניתן לכתוב אליה ישירות מבחוץ, רק דרך הטריגר) שמחובר ל-17 הטבלאות הרלוונטיות. שימו לב: כמו עם היסטוריית סטטוס הצ׳קים — זה מתעד רק מעכשיו והלאה, אין דרך לשחזר שינויים שקרו לפני שהיומן הופעל.

### בדיקה: תנועות ידניות שלא נראו בדוח מחלקה
- נבדק ישירות מול מסד הנתונים: כל הרישומים הידניים שהוזנו (חב"ד בעליה, דוברי צרפתית, ההעברה הבין-מחלקתית שנוסתה) קיימים ותקינים לגמרי — מחלקה נכונה, תאריך נכון, סטטוס "מאושר". קוד הדוח שולף אותם נכון. כל דפי הדוח בנויים כדפים דינמיים לחלוטין (נבדק ב-build) — כלומר בכל טעינה הם פונים ישירות למסד הנתונים בלי קאש בצד השרת, כך שלאחר רענון מלא של הדף (לא רק ניווט) אמורים תמיד להופיע נתונים עדכניים. אם זה עדיין לא מופיע אחרי רענון מלא (Ctrl+Shift+R) — נא לשלוח שוב עם שם המחלקה הספציפית שבה זה קורה, כדי לאתר את זה במדויק.
- כדי שתקלה כזו לא תישאר שקטה בעתיד: הוספתי רישום שגיאה (log) אם אחת מהשאילתות שמרכיבות את דוח המחלקה נכשלת — קודם זה היה נבלע בשקט ומוצג כאילו פשוט "אין תנועות".

### הנפקה מהירה — הפרדה ברורה בין חשבונות בנק
- טבלת "הנפקה מהירה" (מספור רציף של צ'קים) מקובצת עכשיו לפי חשבון בנק עם כותרת צבעונית לכל קבוצה, בדיוק כמו שאר הטבלאות בדף הצ'קים — כשיש כמה חשבונות באותה רשימה, רואים בבירור איפה נגמר חשבון אחד ומתחיל הבא.
- תיקון נלווה: המספור הרציף (ו"עד כאן הונפק"/"דלג על מספר") רץ עכשיו בנפרד לכל חשבון בנק במקום ברצף אחד גורף על פני כל הרשימה — כדי שמספרי צ'קים של חשבון אחד לא "יזלגו" בטעות למספור של חשבון אחר כשהם מעורבבים ברשימה.

### חדש: כפתור "העברה בין מחלקות"
- כפתור חדש (לצד "+ הכנסה / הוצאה" בדשבורד, ב-/ledger וב-/checks) לרישום שמחלקה אחת חייבת לשנייה — בוחרים מחלקה חייבת, מחלקה זכאית, סכום ותאריך.
  - אם לשתי המחלקות אותו חשבון בית: נרשמות שתי תנועות ידניות דרך אותו חשבון (הוצאה לחייבת, הכנסה לזכאית) — הקצאה פנימית בלבד, בלי תנועת בנק אמיתית.
  - אם החשבונות שונים: נרשמת הוצאה אחת למחלקה החייבת, אבל דרך חשבון הבנק של המחלקה הזכאית — מה שגורם למנגנון הקיים שכבר יוצר חוב בין-מחלקתי אוטומטי (אותו מנגנון שעובד היום בהזנה ידנית רגילה) ליצור את החוב הנכון בין המחלקות, בלי צורך בלוגיקה חדשה במסד הנתונים.

### תיקון חמור: אין דרך לדעת מי ומתי סימן צ׳ק כ"נפרע" — נוסף לוג ביקורת
- דווח שצ׳ק סומן כ"נפרע" בלי שהמשתמש זוכר שסימן אותו. בבדיקה לא נמצא בלוג הזמין (שמור 24 שעות אחורה בלבד) תיעוד של הפעולה, כי היא כנראה קרתה מוקדם יותר. **לא הייתה שום דרך במערכת לבדוק מי/מתי שינה סטטוס של צ׳ק** — נוסף `check_status_history`: טבלת ביקורת שנשמרת אוטומטית בכל שינוי סטטוס (מ-, ל-, מי, מתי), מוצגת עכשיו בחלון "פרטים" של כל צ׳ק/העברה. שימו לב: זה מתעד רק שינויים **מעכשיו והלאה** — אין דרך לשחזר מה שקרה לפני שהתיעוד הזה נוצר.
- החשד הסביר ביותר למקרה הספציפי: פאנל "התאמת בנק" מתאים ומסמן צ׳קים כ"נפרע" בבת אחת לפי מספר צ׳ק + סכום מדבקה של דף בנק — אם שורה בדף הבנק תאמה את הצ׳ק הזה, הוא יכול היה להיכלל בסימון קבוצתי בלי אישור נפרד לכל צ׳ק.

### תיקון דחוף: סימון צ׳ק כ"נפרע" עדכן אוטומטית את יתרת הבנק
- נמצא טריגר ישן במסד הנתונים (`fn_check_after_write`) שהוסיף/הפחית אוטומטית מ-`current_balance` של חשבון הבנק בכל פעם שסטטוס צ׳ק השתנה ל/מ-"נפרע" — בניגוד לעיקרון שכבר קיים במערכת (וכתוב במפורש בקוד): עדכון יתרת בנק הוא **ידני בלבד**, גם הכנסות לא נוגעות בו אוטומטית. תוקן — הטריגר ממשיך לעדכן רק את `cleared_at` (מתי הצ׳ק נפרע בפועל), בלי לגעת ביתרת הבנק.
- שימו לב: אם היתרה הנוכחית כבר הושפעה מהבאג הזה, יש לתקן אותה ידנית פעם אחת דרך הכפתור לעדכון יתרה בעמוד התחזית — לא ביצעתי תיקון רטרואקטיבי אוטומטי כי רק אתם יודעים מה היתרה הנכונה כרגע.

### הזנת הכנסה/הוצאה ידנית — כמה שורות ברצף
- כפתור "+ הכנסה / הוצאה" נפתח עכשיו לטבלת שורות (כמו שאר טפסי ההזנה המרוכזת באפליקציה) — כל שורה עם מחלקה/סוג/סכום/תאריך/הערות/חשבון בנק משלה, ושמירה אחת שולחת את כולן (שורה שנכשלת לא חוסמת את השאר).

### דף "כל התנועות" — הסרת פילטר קטגוריה לא רלוונטי + כפתור קיפול לתיבת הסינון
- הוסר פילטר "קטגוריות" מתיבת הסינון העליונה (לא היה רלוונטי לתצוגה המאוחדת).
- תיבת הסינון כולה ניתנת לקיפול/פתיחה עם חץ (נפתחת כברירת מחדל).

### עריכת תנועות (הכנסה/הוצאה) מדף "כל התנועות" (מנהל בלבד)
- כל שורה בטבלת "כל התנועות" מקבלת כפתור "עריכה" למנהל: הכנסה נפתחת בטופס עריכת הכנסה חדש (תורם, סכום, תאריך, קטגוריה, מקור, סוג, מס׳ קבלה/הוראה, הערות); צ׳ק/העברה/רישום ידני נפתחים באותו טופס עריכה שכבר קיים בדף ההוצאות (כולל פיצול בין מחלקות, חשבונית, סימון "ישן").
- נוספה פעולת שרת `updateIncome` — עורכת פרטי הכנסה בלי לגעת במחלקה (שינוי מחלקה נשאר ב-`updateIncomeDepartment` הקיים), כדי לא להתנגש עם הטריגר במסד הנתונים שגוזר את המחלקה מהקטגוריה בכל עדכון שנוגע בה.

### תיקון נוסף לאותה בעיית קאש: צ׳קים/הכנסות
- אותה בעיית קאש שתוקנה עבור רישום ידני (הפריט מופיע בכל מקום חוץ מדוח המחלקה שלו) תוקנה גם עבור עדכון/מחיקה/שיוך מחדש של הכנסה וגם עבור עדכון צ׳ק/העברה — כל פעולה כזו מרעננת עכשיו גם את `/reports/[מחלקה]` הרלוונטי (כולל שתי המחלקות אם התנועה עברה בין מחלקות).

### היסטוריית שם (ספק/תורם) בדוח מחלקה מוגבלת לאותה מחלקה בלבד
- לחיצה על שם ספק/תורם מתוך דוח מחלקה ספציפי (גם ע"י מנהל) מציגה עכשיו רק את התנועות של אותה מחלקה, במקום את כל המערכת — כדי שהמסך יישאר עקבי עם ההקשר שממנו נפתח.

### ייצוא דוח מחלקה לאקסל
- כפתור "ייצוא לאקסל" חדש בראש כל דוח מחלקה (גם ב-/reports/[מחלקה] וגם ב-/ledger כשבוחרים מחלקה) — מוריד קובץ .xlsx עם 3 גיליונות: תנועות עד היום, תנועות עתידיות, ותזרים חודשי. אותו מקור נתונים בדיוק כמו הדוח על המסך (חולץ ל-`src/lib/department-report-data.ts` כדי שהם לא יוכלו להתפצל זה מזה).
- נוספה תלות חדשה — `exceljs` ליצירת קבצי אקסל. נבדק מול `xlsx` (SheetJS) שהוחרג בגלל שתי חולשות אבטחה בדרגה גבוהה ללא תיקון זמין ברג'יסטרי npm; ל-`exceljs` יש חולשה בינונית עקיפה (תלות ב-`uuid` ישן) שאינה רלוונטית לאופן שבו היא נקראת כאן (רק ליצירת מזהים פנימיים, לא עם הפרמטר הפגיע).

### תיקון: רישום ידני (הכנסה/הוצאה) לא הופיע מיד בדוח המחלקה שלו
- כשמאשרים/יוצרים רישום ידני, ה-cache לא היה מתעדכן עבור עמוד הדוח של המחלקה הספציפית (`/reports/[id]`) ולא עבור `/ledger` — רק עבור הדשבורד/הוצאות/תנועות. תוקן: כל פעולה על רישום ידני (יצירה, עדכון, מחיקה, אישור/דחייה) מרעננת גם את דוח המחלקה הרלוונטית וגם את `/ledger`.

### תיקון תאריך עמלת אשראי
- עמלת האשראי החודשית (2% מהכנסות אשראי/ביט/העברה בקליק) מוצגת ונספרת עכשיו בתאריך ה-1 לחודש שאחרי חודש ההכנסה שממנה היא מחושבת — לא ב-1 לאותו חודש.

### דוח מחלקה: סינון הכנסות/הוצאות, "סוג" מלא, פרטים+היסטוריית שם בלחיצה
- בשתי הטבלאות בדוח מחלקה ("תנועות עתידיות ידועות" ו"תנועות עד היום") נוסף כפתור סינון "כל התנועות / הכנסות בלבד / הוצאות בלבד" ליד בורר החודש.
- שורת הכנסה בטבלה מציגה עכשיו גם את ה"סוג" (מספר תשלום כמו "1/2", או טקסט חופשי כמו "הו״ק") ולא רק את ה"מקור" (למשל "אשראי").
- לחיצה על שם התורם/המוטב (או על "פרטים") פותחת פירוט מלא של התנועה, וגם רשימה של כל התנועות האחרות תחת אותו שם בדיוק — לתשלומים (היה כבר קיים) ולהכנסות (חדש).

### מטריצת יתרות נטו: תקציר דו-עמודתי + טבלת פירוט נפרדת
- "מטריצת יתרות נטו" ב-/ledger הפכה לטבלה דו-עמודתית: שם מחלקה + יתרה נטו כוללת שלה (זכאית/חייבת), במקום הרשימה הזוגית (מי-מול-מי).
- מתחתיה נוספה טבלה נפרדת "פירוט — מי חייב למי" עם הפירוט הזוגי המקורי (חייב / זכאי / סכום נטו).

### הסרת כפתורי "דוחות מחלקתיים" כפולים בדשבורד ובניהול מחלקות
- הוסרה רשימת הקישורים "דוחות מחלקתיים" (כפתור לכל מחלקה) גם מהדשבורד הראשי וגם מדף "ניהול מחלקות" — היו מיותרים לצד הטבלה עצמה.
- במקום זאת, שם המחלקה בטבלת "ניהול מחלקות" הוא עכשיו קישור ישיר לדוח של אותה מחלקה.

### בדיקת דיווח על כפתור "קליטת הכנסות בהדבקה"
- נבדק מול לוגים ומול טבלת ההכנסות בפועל: הפעולה עבדה — נשמרו הכנסות אמיתיות בדקות שלפני הבדיקה, כולל שיוך מחלקה/חשבון בנק/קטגוריה תקין וטקסט "סוג" מלא. לא נמצאה שגיאת שרת. ייתכן שמדובר בטעינה ישנה של הדף (cache) מלפני הפריסה האחרונה — אם זה חוזר, נדרש עוד מידע (צילום מסך/הודעת שגיאה מדויקת).

## 2026-08-24 (המשך 3)

### הדבקת הכנסות: שורות שדורשות בדיקה כבר לא נכפלות אחרי שמירה, ואפשר לדלג על שורה
- תוקן באג נוסף שהתגלה תוך כדי: אחרי לחיצה על "שמור", כל שורה שנשארה לטיפול (עדיין לא תקינה) הופיעה בטבלה **פעמיים** — קוד השמירה הוסיף אותה גם ב-map הרגיל וגם בנפרד ב-concat מיותר. תוקן כך שכל שורה שלא נשמרה נשארת בדיוק פעם אחת, בלי לגעת בה.
- נוספה עמודת פעולה חדשה עם כפתור "✕ דלג" בכל שורה — מסיר אותה מהתצוגה המקדימה בלי לשמור, לשורות שלא שווה לתקן/לשייך.

### הדבקת הכנסות: עמודת "סוג" מציגה את הערך המקורי גם כשזה לא X מתוך Y
- כשהעמודה "סוג" לא מתפרשת כמספר תשלומים (למשל "הו״ק" — הוראת קבע), הטבלה כבר לא מציגה שני תיבות מספר ריקות שנראות כאילו שום דבר לא נקלט — היא מציגה את הטקסט המקורי שהודבק, בדיוק כמו שהוא נשמר בעמודה `type_text`.

## 2026-08-24 (המשך 2)

### תיקון דחוף: דף "ניהול מחלקות" קרס בכל טעינה
- לחיצה על "ניהול מחלקות" (בהגדרות) גרמה לשגיאת שרת ("This page couldn't load") בכל פעם — לא תקלה מזדמנת. הסיבה: פונקציית עזר (`usageFor`) חושבה בקומפוננטת השרת של הדף והועברה כפרופ ישירות לרכיב לקוח ("use client") — Next.js אוסר להעביר פונקציות רגילות דרך הגבול הזה. תוקן על ידי חישוב הנתונים בתוך רכיב הלקוח עצמו (מקבל את רשימת הקטגוריות/חשבונות הבנק כמידע גולמי במקום פונקציה). אומת שהדף חוזר להיטען כרגיל.

### תיקון דחוף: כתובת עם פסיק בהדבקת הכנסות מזיזה את כל העמודות
- שורה ממשית שהודבקה כללה כתובת עם פסיק בתוכה ("...עפולה,גבעת המורה") — מנגנון הפיצול של שורה מודבקת פיצל גם לפי פסיקים (בנוסף לטאבים), כדי לתמוך בפורמט ישן. זה גרם לכתובת להיחתך לשתיים ולהזיז את כל העמודות שאחריה בעמודה אחת, כך שהמערכת ניסתה לשייך את כתובת המייל כקטגוריה במקום את הקטגוריה האמיתית. תוקן: שורה שמכילה טאב מפוצלת אך ורק לפי טאבים (הפסיקים בתוך השדות החופשיים לא נוגעים בה יותר); פיצול לפי פסיקים נשאר רק כברירת מחדל לפורמט ישן שאין בו טאבים כלל.

### עמלת אשראי אוטומטית + שימור מלא של עמודת "סוג" בהדבקת הכנסות
- נוספה טבלה חדשה (`credit_commission_entries`) שמתעדכנת אוטומטית ע"י טריגר על טבלת ההכנסות: לכל מחלקה ולכל חודש, שורת "עמלת אשראי" אחת המחושבת כ-2% מסך ההכנסות של אותו חודש שמקורן אשראי/ביט/העברה בקליק. השורה מופיעה בדוח המחלקה (מצב נוכחי, תזרים חודשי, ורשימת התנועות) בדיוק כמו הוצאה רגילה — אך אינה ניתנת לעריכה ידנית, מתעדכנת אוטומטית ככל שנכנסות הכנסות חדשות באותו חודש.
- הפונקציות שמתחזקות את הטבלה הזו ננעלו מפני קריאה ישירה דרך ה-API הציבורי (anon/authenticated) — הן מיועדות לרוץ רק דרך הטריגר הפנימי, בהתאם לכלל האבטחה שפונקציית SECURITY DEFINER לא צריכה להיות פתוחה לקריאה חיצונית בלי סיבה מפורשת.
- עמודת "סוג" בהדבקת הכנסות (למשל "1/10" לתשלומים, אבל גם "העברה"/"מזומן" וכדומה) נשמרת עכשיו במלואה בעמודה ייעודית (`type_text`) בטבלת ההכנסות — לא רק כשהיא מתפרשת כמספר תשלומים, אלא תמיד, לטובת דוחות עתידיים (צפי ותזרים, הוראות קבע).

### הדבקת הכנסות: שורות בעייתיות מרוכזות למעלה לפני השורות התקינות
- בתצוגה המקדימה של הדבקת הכנסות, השורות שדורשות בדיקה/אישור (מבוטלות, כפולות, קטגוריה לא משויכת, סכום לא תקין, או שנכשלו בשמירה קודמת) מוצגות עכשיו תמיד בראש הרשימה תחת כותרת "⚠ דורש בדיקה ואישור", ורק אחריהן כל השורות שנדבקו בסדר תחת "✓ נדבק תקין" — כדי לעבור על מה שדורש טיפול קודם, בלי לחפש אותו מפוזר בין כל שאר השורות.

### הדבקת הכנסות: חובת בחירת חשבון בנק, אישור לפני שמירה, ובדיקת קטגוריות מיד עם ההדבקה
- תיבת ההדבקה נעולה עד שבוחרים חשבון בנק יעד למעלה — אי אפשר להתחיל להדביק/לעבוד על השורות לפני שברור לאיזה חשבון הן שייכות.
- לחיצה על "שמור" מציגה עכשיו הודעת אישור עם מספר השורות התקינות שיישמרו ומספר השורות שנשארות למטה לטיפול, לפני שהשמירה בפועל מתבצעת.
- ברגע ההדבקה, אם יש קטגוריות שלא קיימות/לא משויכות למחלקה, מופיעה מיד תיבת אזהרה בולטת בראש התצוגה המקדימה עם רשימת הקטגוריות הללו ובורר מחלקה לכל אחת — כדי לשייך אותן במקום אחד לפני שממשיכים הלאה בבדיקת שאר השורות.

## 2026-08-23 (המשך 8)

### תחזית תזרים: איחוד כרטיס החודש עם הפירוט היומי שלו
- שני האזורים שהיו נפרדים בתחתית דף התחזית — "סיכום יומי" אחד גדול לכל הטווח, ואחריו בנפרד "מצב חזוי לפי חודשים" — אוחדו: עכשיו כל כרטיס חודש מלווה מיד בטבלת הפירוט היומי שלו בלבד, חודש אחרי חודש ברצף אחד, במקום שני בלוקים נפרדים.
- שינוי ניסוח: "הוצאות ישנות שלא נפדו מחודשים קודמים" → "הוצאות מתאריכים קודמים שלא נפדו".

## 2026-08-23 (המשך 7)

### תיקון דחוף: דף תחזית תזרים לא טען בכלל
- העדכון הקודם (הוספת צ'קים שטרם הונפקו + שורת סיכום לצ'קים ללא תאריך) הכניס שגיאת SQL אמיתית לפונקציית `get_cash_flow_forecast` — שם עמודה כפול (`running_balance`) שהתנגש בין פרמטר הפלט של הפונקציה לעמודה בשאילתה הפנימית, וגרם ל-Postgres לזרוק שגיאה בכל קריאה. זו הסיבה שדף התחזית הציג "אירעה שגיאה בשמירה" ולא טען את רשימת פירוט התנועות למטה כלל. תוקן ואומת ישירות מול בסיס הנתונים (גם עם צ'קים רגילים וגם עם צ'ק ללא תאריך) שהפונקציה מחזירה נתונים תקינים.

## 2026-08-23 (המשך 6)

### תחזית בנק: צ'קים מאושרים שטרם הונפקו, וצ'קים ללא תאריך כשורת סיכום נפרדת
- עד כה, תחזית התזרים כללה רק צ'קים מאושרים עם תאריך שכבר קיבלו מספר צ'ק (הונפקו) — צ'ק מאושר עם תאריך יעד שעדיין לא הונפק לא הופיע בתחזית בכלל. תוקן: כל צ'ק/העברה מאושרים עם תאריך יעד נכנסים עכשיו לתחזית, גם אם טרם הונפקו — זו עדיין התחייבות אמיתית לתאריך ידוע.
- צ'קים/העברות מאושרים **ללא** תאריך יעד כלל (עדיין לא נקבע מתי) מקבלים עכשיו טיפול נפרד: הם לא משויכים לאף יום/חודש ולא נכנסים לחישוב היתרה החזויה או לסיכום היומי/חודשי הרגיל, אלא מוצגים כשורת סיכום עצמאית בראש דף התחזית — "צ'קים/העברות מאושרים ללא תאריך יעד" — עם הרשימה והסכום הכולל, כדי שההתחייבות תישאר גלויה בלי לעוות את התחזית לפי תאריכים.

## 2026-08-23 (המשך 5)

### סימון "ישן" בכמה שורות יחד + אזהרה לפני דריסת שיוך קיים
- בטבלת "כל ההוצאות", כשבוחרים כמה צ'קים/העברות יחד (אותו מנגנון בחירה מרובה שכבר משמש לשיוך מחלקה בבת אחת), נוספו שני כפתורים: "סמן כישן — לא נכלל" ו"סמן כן נכלל" — מעדכנים את כל הנבחרים בבת אחת בלי לפתוח את חלון העריכה לכל אחד בנפרד.
- בכל הפעולות שמשנות שיוך קיים על כמה תנועות בבת אחת — עדכון מחלקה לנבחרים, קיבוץ לפריסה, חלוקה למחלקות מהסכום הכולל — נוספה אזהרה שמופיעה רק כשבאמת יש מה לאבד: אם חלק מהנבחרים כבר משויכים למחלקה ו/או כבר חלק מפריסה קיימת, מוצגת הודעה שמפרטת בדיוק כמה מהם ומה ישתנה, לפני שהפעולה מתבצעת.

## 2026-08-23 (המשך 4)

### תיקון קריטי: רשימות נפתחות שנעלמו מאחורי חלונות מודאליים
- אותר ותוקן באג נוסף, חמור יותר, באותו נושא: כל רשימה/תפריט שנבנה על Radix UI ונפתח מתוך חלון מודאלי (למשל "חשבון בנק" בטופס "דרישת תשלום חדשה") התמקם "מאחורי" החלון ולא היה נגיש בכלל — בדיוק כמו שתואר ("מתחבאת מאחורי החלון"). הסיבה: חלון המודאל בנוי על אלמנט `<dialog>` שהדפדפן מרנדר ב"שכבת-על" נפרדת מעל כל שאר העמוד, ואילו Radix מציב את הרשימות הנפתחות שלו כברירת מחדל ישירות בסוף ה-`<body>` — מחוץ לשכבת-העל הזו, ולכן מתחת לחלון ויזואלית. נבנה מנגנון כללי (`usePortalContainer`) שמזהה אם הכפתור נמצא בתוך חלון מודאלי פתוח, ובמקרה כזה מציב את הרשימה בתוכו במקום ב-`<body>` — תוקן בכל שלושת המקומות (תפריט פעולות שורה, סינון עמודות, בורר עם חיפוש).
- תוקנה גם התנהגות מבלבלת בבורר עם חיפוש (SearchableSelect): כשפותחים שדה שכבר יש בו ערך נבחר, הרשימה נפתחה "מסוננת מראש" רק לפי הערך הקיים (מוצג כ"1 מתוך 17 תוצאות") — נדרש היה למחוק טקסט כדי לראות אפשרויות אחרות. עכשיו הרשימה המלאה תמיד מוצגת מיד עם הפתיחה, ואפשר להתחיל להקליד כדי לסנן.
- בנוסף לבורר בקלט חופשי, עדיין אפשר לנווט ולבחור עם החצים ו-Enter בלי להקליד כלל — מתאים גם לרשימות קצרות.

### עריכת צ'ק/העברה: ביטול חובת בחירת מחלקה + הוספת פיצול/חשבונית/סימון כישן
- בעריכת צ'ק או העברה קיימים (מתוך "צ'קים והעברות"), המערכת דרשה עד כה לבחור מחלקה כדי לשמור. הדרישה בוטלה — בדיוק כמו ביצירת צ'ק/העברה חדשים, מותר להשאיר "ממתין לסיווג" ולשייך למחלקה מאוחר יותר.
- נוספה לחלון העריכה אפשרות "פיצול מחלקה" — מסמנים ומקבלים עורך פיצול בין כמה מחלקות (כמו ביצירה), במקום בורר מחלקה בודד.
- נוספו לאותו מקום גם "יש חשבונית" ו"סימון כישן (לא לכלול במאזן המחלקה)" — כך שאפשר לעדכן את שלושת הסימונים האלה ישירות מחלון העריכה, בלי צורך בפעולה נפרדת לכל אחד.

## 2026-08-23 (המשך 3)

### תיקון שורש: תפריטים/רשימות שקפצו למקום אחר בעמוד
- אותרה הסיבה האמיתית לדיווחים החוזרים על תפריטים ורשימות שנפתחים "במקום הלא נכון": אנימציית הכניסה של כל `.card` (המסגרת שכל טבלה/כרטיס בעמוד עטופה בה) הסתיימה ב-`transform: translateY(0)` במקום `transform: none`. גם `translateY(0)` שלא זז בפועל נחשב "transform" לפי תקן ה-CSS, וזה הופך כל `.card` לנקודת ייחוס עבור אלמנטים עם `position: fixed` בתוכו — כלומר כל תפריט/רשימה שהשתמשו בטריק הזה (תפריט פעולות שורה, סינון עמודות, בורר עם חיפוש) התמקמו ביחס לכרטיס במקום ביחס למסך, ולפעמים "קפצו" רחוק. תוקן ב-CSS אחד.
- מעבר לפתרון מקצועי ויציב יותר לטווח ארוך: שלושת מנגנוני התפריט/רשימה הנפתחים (תפריט פעולות שורה, תפריט סינון עמודה, בורר עם חיפוש) נבנו מחדש על גבי Radix UI (אותה ספרייה שמאחורי shadcn/ui, בדיוק כמו שביקשת בהשוואה לתוכנת השידוכים) — עיגון אמיתי לכפתור המפעיל עם זיהוי התנגשות עם קצוות המסך, אנימציית פתיחה/סגירה עדינה, וניווט מקלדת (חצים + Enter) בבורר עם החיפוש שלא היה קיים קודם.
- הכפתורים עצמם גדלו ומקבלים עיצוב אחיד: כפתור התפריט בטבלת ההוצאות הפך לעיגול 32×32 פיקסלים עם שלוש נקודות, ופריטי התפריט (עריכה/ביטול/מחיקה/פרטים) מקבלים כעת שטח לחיצה מלא ורקע בהעברת עכבר במקום טקסט קטן עם קו תחתון.
- נבדק ישירות בדפדפן (לא רק בקוד) לפני שילוח — כולל מסך צר בדיוק כמו בצילומי המסך ששלחת, ווידוא שהתפריט נשאר צמוד לכפתור בכל שורה.

## 2026-08-23 (המשך 2)

### תפריט פעולות מתקפל בטבלת ההוצאות
- בדף ההוצאות, עמודת הפעולות ("פרטים", "עריכה", "ביטול", "מחיקה") שהייתה פזורה כקישורים נפרדים ברוחב השורה הפכה לכפתור חץ אחד (⌄) שפותח תפריט נפתח עם כל הפעולות — כמו תפריט הסינון בכותרות הטבלה, כולל בריחה מחיתוך הגלילה האופקית.

## 2026-08-23

### סימון "יש חשבונית" על צ'קים והעברות
- בטופס "דרישת תשלום חדשה" (יצירת צ'ק/העברה חדשים) נוספה תיבת סימון "יש חשבונית" — חלה על כל השורות כשיוצרים פריסה לכמה תשלומים.
- בדף "כל התנועות" נוספה עמודת "חשבונית" עם מיון וסינון (בסגנון אקסל, כמו שאר העמודות) — רלוונטית רק לצ'קים/העברות, מוצגת כ-"—" בהכנסות וברישומים ידניים.
- מנהל כספים יכול לתקן את הסימון ישירות מתוך עמודת "חשבונית" בטבלה (בלי לפתוח את חלון העריכה המלא) — לדוגמה כשחשבונית מגיעה מאוחר יותר או כשהתברר שלא הייתה בכלל.

## 2026-08-22 (המשך 5)

### בדיקת אבטחה חוזרת — סריקה מלאה של 20 הנקודות
- נמצא ותוקן: שתי פעולות שרת בדף ההכנסות (`checkExistingTransactionRefs`, `submitIncomeBatch` — שמירת הדבקת הכנסות בפועל) לא קראו במפורש ל-`requireUser()`/`requireFinanceAdmin()`, בניגוד לכלל שנקבע בפרויקט. ה-RLS כבר חסם בפועל הזנת הכנסות על ידי מי שאינו מנהל כספים, כך שלא הייתה כאן פרצה מנוצלת בפועל — אבל זה עדיין חריגה מהכלל של "הגנה כפולה", ותוקן.
- שאר 19 הנקודות נבדקו מחדש אחד־אחד ישירות מול הקוד/מסד הנתונים החי ונמצאו תקינים, למעט שתי נקודות שכבר סומנו כפתוחות במודע: הגנת סיסמאות דלופות (Leaked Password Protection) עדיין דורשת הפעלה ידנית בדשבורד של Supabase, והגבלת קצב (Rate limiting) עדיין דורשת תשתית חדשה או אישור מפורש להוספתה.

## 2026-08-22 (המשך 4)

### דוח מחלקה — בנייה מחדש: מצב נוכחי / עתידי / עבר, כל אחד עם סינון וסיכום
- בחירת המחלקה בראש "דוחות מחלקות" הפכה לרשימה נפתחת עם חיפוש (SearchableSelect), במקום שורת כפתורי מחלקה — מוצגת תמיד, גם לפני שנבחרה מחלקה.
- הדוח עצמו נבנה מחדש לשלושה חלקים ברורים, בסדר הזה: **מצב נוכחי** (שלושת הכרטיסים — הכנסות/הוצאות/יתרה, לפי מה שקרה בפועל עד היום), **תנועות עתידיות ידועות**, ואז **תנועות עד היום** (היסטוריה מלאה).
- שני חלקי התנועות (עתידי ועבר) מקבלים עכשיו בורר "חודש" ובורר "תאריך מדויק" משלהם — בחירה מסננת גם את שורת הסיכום (הכנסות/הוצאות/נטו) למעלה וגם את הטבלה שמתחתיה, בלי לגעת בשאר הדוח.
- כל טבלאות הדוח (עתידי ועבר) ממוינות כברירת מחדל לפי תאריך — עתידי מהקרוב לרחוק, עבר מהחדש לישן — עם חץ המיון הפעיל מסומן בכותרת העמודה מההתחלה.
- "תזרים חודשי מלא — עבר ותחזית" (כולל הוראות קבע שעוד לא הגיעו לתאריך שלהן) נשאר ככרטיס מתקפל נפרד בין חלק העתידי לחלק העבר, לצפייה מעמיקה יותר כשצריך.

## 2026-08-22 (המשך 3)

### דוח תזרים — רק תזרים בנק, ללא הקשר מחלקתי
- דף "תחזית תזרים" חדל להציג "תחזית מחלקה" — נשארה רק תחזית חשבון בנק (לא היה בה שימוש בפועל אחרי שדוח המחלקה קיבל תזרים חודשי משלו, ראו למטה). הקישור "תחזית מחלקתית מלאה" בדוח המחלקתי המודפס הוסר.
- הדף (וגם קישור התפריט העליון) מוסתר לגמרי ממנהל מחלקה שהמחלקה שלו לא מנהלת חשבון בנק משלה (רוב המחלקות פועלות דרך חשבון "פעילות מרכזית") — אין לו מה לראות שם. מנהל כספים ממשיך לראות הכל כרגיל.
- מנהל מחלקה שכן מנהלת חשבון בנק רואה בבורר רק את החשבון/ות ששייכים למחלקה שלו, ולא את כל חשבונות הבנק של הארגון כפי שהיה קודם — גם אם מנסים לשנות את זה ידנית בכתובת ה-URL.

### דוח מחלקה — פיצול לפני/אחרי היום, מאוחד עם התזרים
- "כל התנועות" בדוח מחלקה פוצל לשתי טבלאות: **תנועות עד היום** (מה שכבר קרה בפועל) ו**תנועות עתידיות ידועות** (צ׳קים/העברות שכבר קיימים במערכת עם תאריך עתידי), עם שורת סיכום הכנסות/הוצאות לחלק העתידי.
- שלושת הכרטיסים למעלה ("סה״כ הכנסות/הוצאות", "זכאית/חייבת") מחושבים כעת רק לפי **עד היום** — מצב החשבון האמיתי כרגע, בלי לכלול בטעות התחייבויות עתידיות שעוד לא הגיעו.
- ה"תזרים חודשי — עבר ותחזית" שנוסף קודם כרכיב נפרד הועבר להיות חלק מאותו אזור "תנועות עתידיות", ולא כרטיס נפרד שצף למטה — יחד עם תיקון באג שגרם לספירה כפולה של תנועות עם תאריך עתידי (פעם מהרשימה בפועל ופעם מהתחזית).

## 2026-08-22 (המשך 2)

### דוח מחלקה — תזרים חודשי (עבר + תחזית), כמו דוח תזרים בנק
- בכל דוח מחלקה (`/ledger?department=...` וגם דוח ההדפסה `/reports/[מחלקה]`) נוסף אזור מתקפל חדש "תזרים חודשי — עבר ותחזית" מתחת לרשימת התנועות — סגור כברירת מחדל, כך שהתצוגה הרגילה (הכרטיסים למעלה + רשימת התנועות) נשארת בדיוק כפי שהייתה.
- בפתיחה מוצגת טבלה של יתרת פתיחה/הכנסות/הוצאות/יתרת סגירה לכל חודש — רצף אחד ורציף שמתחיל בהיסטוריה האמיתית (מה שכבר נרשם), וממשיך בלי הפסקה לתוך אותה תחזית שמבוססת על הוראות קבע וצ'קים/העברות ידועים שדף התחזית מציג.
- ניתן לבחור "הצג החל מחודש" (רק חודשי עבר ברשימה) — הטבלה מתקצרת להתחיל משם, ומופיעה שורת סיכום "יתרה מצטברת עד סוף [חודש]" עם הסכום שהמחלקה הייתה חייבת/זכאית נכון לאותו רגע.

## 2026-08-22

### השלמת בדיקת האבטחה — סגירת שני הממצאים האחרונים
- **CORS** ב-Edge Function ליצירת משתמשים (`admin-create-user`): הוסרה ההרשאה הפתוחה `Access-Control-Allow-Origin: *` והוחלפה בהגבלה למקור האפליקציה בלבד (`dashboard-production-dcba.up.railway.app`).
- **חוזק סיסמה** באותה פונקציה: הועלה מ-6 תווים בלבד (בלי דרישת תוכן) ל-10 תווים לפחות עם חובת אות וספרה.
- באותה הזדמנות: הודעת השגיאה שהוחזרה ללקוח כשיצירת המשתמש נכשלת בשרת כבר לא מציגה את הודעת ה-DB/Auth הגולמית של Supabase (חוץ ממקרה של דוא"ל כפול, שנשאר עם הודעה ברורה) — כל שגיאה אחרת נרשמת ביומן השרת ומוצג ללקוח נוסח כללי בעברית.
- נבדקו מול ה-Advisors העדכניים של Supabase: פונקציות הקיוסק (שכבר הועברו ל-Vault בשבוע שעבר) עדיין מסומנות כ"נגישות ל-anon" — זה מכוון (הסוד עצמו הוא שכבת האימות שלהן) ואינו דורש תיקון נוסף. טבלת `print_queue` עם RLS מופעל וללא מדיניות כלל — כלומר חסומה כברירת מחדל לגמרי, גם זה תקין. פונקציות SECURITY DEFINER פנימיות נוספות (`is_finance_admin`, `user_has_department` וכו') מיועדות להיקרא ע"י משתמשים מחוברים ואינן מכילות סוד — תקין.
- **פתוח, דורש פעולה ידנית בדשבורד של Supabase (לא ניתן לתיקון דרך קוד/MCP)**: "Leaked Password Protection" באימות (Auth) עדיין כבויה — Supabase בודק מול HaveIBeenPwned שהסיסמה לא דלפה בעבר. יש להפעיל דרך Dashboard → Authentication → Policies.
- **הגבלת קצב (Rate limiting)**: נשאר פתוח באופן מודע — אין היום תשתית (כמו Redis/Upstash) בפרויקט, וזו הוספת רכיב חדש ולא רק תיקון. אם רוצים, אפשר להוסיף הגבלה פשוטה בתוך ה-Edge Function עצמה (למשל לפי כתובת IP וטבלת עזר במסד הנתונים) — נדרש אישור לפני שמתחילים.

## 2026-08-19 (המשך 18)

### דף "דוחות מחלקות" — רק מטריצת היתרות, שמות מחלקה לחיצים
- הוסרה שורת כפתורי המחלקה שהופיעה תמיד למעלה (הציפה את המסך לפני שהגעת למידע). כשלא נבחרה מחלקה מוצגת רק מטריצת היתרות הנטו, ושם המחלקה (חייב/זכאי) בכל שורה הוא קישור ישיר לדוח שלה. כפתורי המחלקה לחזרה/מעבר מהיר עדיין מופיעים כשכבר צופים בדוח של מחלקה ספציפית.

## 2026-08-19 (המשך 17)

### פישוט בחירת תדירות ביצירת/עריכת הוראת קבע
- הוסרו אפשרויות "שבועי"/"שנתי" מטופס יצירת/עריכת הוראת קבע — נשארו רק "חודשי" ו"חד פעמי" (לא היו הוראות קבע קיימות מהסוגים שהוסרו). ביום בחודש: במקום שדה מספרי, נבחר תאריך מלא בלוח שנה, והיום בחודש שממנו נגזר החיוב החוזר.

## 2026-08-19 (המשך 16)

### פירוק פריסה קיימת לפריסה נפרדת
- נוסף כפתור "פרק לפריסה נפרדת" בדף ההוצאות (בבחירה מרובה של צ׳קים) — מוציא את הצ׳קים הנבחרים מתוך הפריסה הקיימת שלהם ליוצר עבורם פריסה חדשה ונפרדת. שיוך המחלקה מתאפס ל״ממתין לסיווג״ גם על הנבחרים וגם על שאר הצ׳קים שנשארים בפריסה המקורית (כי הסכומים חושבו יחד ואיבדו משמעות ברגע הפירוק). כולל אזהרה ברורה שהפעולה בלתי הפיכה לפני ביצוע.

## 2026-08-19 (המשך 15)

### עריכת כלל הוראת קבע קיימת
- נוסף כפתור "עריכה" לכל שורה בטבלת "הוראות קבע" — פותח טופס מלא (אותו טופס כמו יצירה חדשה) עם כל השדות הקיימים ממולאים מראש: שם, מחלקה/פיצול, סוג, תדירות, תאריך, סכום צפוי, חשבון בנק, קטגוריה, הגבלת זמן. שינויים חלים רק על מופעים עתידיים — מה שכבר נרשם בפועל (manual_department_entries) לא נוגע.
- הערה: עריכת סכום נקודתית לחודש בודד (בלי לגעת בכלל) כבר קיימת דרך "אישור סכומים בפועל" (להוראות משוער) ודרך כפתור "עריכה" בטבלת ההוצאות (לרשומות שכבר נרשמו) — לא נדרש שינוי נוסף.

## 2026-08-19 (המשך 14)

### תיקון: הוראות קבע מפוצלות בין מחלקות לא יצרו כפתור "יצא"/רישום אוטומטי
- אותרה תקלה: כל הפונקציות שמזהות הוראת קבע שצריך לאשר/לרשום (`get_pending_schedule_confirmations`, `materialize_known_recurring_occurrences`) דרשו `department_id` לא ריק — בדיוק מה שאין להוראת קבע **מפוצלת** בין כמה מחלקות (היא נשמרת עם `department_id` ריק + פיצול בטבלה נפרדת). כתוצאה מכך הוראות קבע מפוצלות (כמו "ביטוח לאומי" ו"מס הכנסה" בדוגמה) מעולם לא הציגו את כפתור האישור למרות שהתאריך כבר הגיע, וגם לא נרשמו אוטומטית כשמדובר בסכום קבוע.
- תוקן בשתי הפונקציות: הוראת קבע מפוצלת מזוהה עכשיו כראוי. בחלון האישור (הכנסה/הוצאה עם סכום משוער) היא נפתחת ישר במצב "פיצול בין מחלקות" עם הסכומים הקיימים כברירת מחדל לכל מחלקה. בהוראת קבע מפוצלת עם סכום קבוע, נוצרת אוטומטית שורה נפרדת לכל מחלקה במקום שורה אחת חסרת מחלקה.

## 2026-08-19 (המשך 13)

### הוצאות: מחיקה/ביטול ישירות מהשורה
- כל שורה בטבלת "הוצאות" קיבלה כפתור מחיקה ישיר (לא רק דרך פרטי ההוצאה) — פועל גם על צ׳קים/העברות וגם על רישומים ידניים (נוספה `deleteManualEntry`, שלא הייתה קיימת קודם).
- צ׳ק/העברה קיבלו גם כפתור "ביטול" נפרד מהמחיקה — מסמן כ״בוטל״ (status=CANCELLED) והשורה נשארת ברשימה עם תג "בוטל", במקום להיעלם. דף ההוצאות הפסיק לסנן צ׳קים מבוטלים החוצה כליל.
- נוסף כפתור "רישום מספר צ׳ק כמבוטל" בעמוד "ניהול צ׳קים והעברות" — לרישום מספר צ׳ק שמעולם לא הונפק בפועל (נקרע/התקלקל) כדי שלא ייראה כפער מול הבנק, גם כשאין לו שום שורה קיימת ברשימה.

## 2026-08-19 (המשך 12)

### בדיקת אבטחה מקיפה + תיקונים
- **קריטי**: פונקציות "קיוסק הדפסה" (`create_print_label`, `kiosk_mark_printed`, `kiosk_read_pending_labels`) — פיצ׳ר שאינו קשור כלל לאפליקציה הזו אך יושב באותו מסד נתונים — השוו סוד קבוע בטקסט גלוי בתוך קוד ה-SQL, וניתנות להפעלה ע״י משתמש לא מחובר (anon). הועברו ל-Supabase Vault; הסוד כבר לא חשוף בקוד המקור.
- שש פונקציות שרת ב-`checks/actions.ts` היו נשענות רק על RLS בלי בדיקת הרשאה מפורשת בצד שרת — נוספה `requireUser()`/`requireFinanceAdmin()` לכל אחת. באופן קונקרטי `updateCheckStatus` הייתה עלולה "להצליח" באופן מטעה עבור משתמש לא-מורשה (RLS פשוט מסנן 0 שורות בלי שגיאה) — עכשיו נחסמת מיד.
- הודעות שגיאה גולמיות מה-DB (Postgres/PostgREST) שהוחזרו ישירות למשתמש בעשרות מקומות ב-5 קבצי actions הוחלפו בעטיפה משותפת (`safeErrorMessage`) שמסתירה פרטים פנימיים בפרודקשן.
- הרשאת CORS על ה-Edge Function ליצירת משתמשים תישקל בהמשך להגבלה למקור האפליקציה בלבד.
- דוח מלא של 20 נקודות הבדיקה (כולל הדירוגים וסטטוסים) נמסר בשיחה.

### תיקוני תצוגה ופיצ׳רים
- רכיב "בחירה חכמה" (SearchableSelect): הרשימה הנפתחת חושבה לפי גבולות ה-`div` העוטף ולא לפי תיבת הקלט עצמה — בפריסות גריד/פלקס זה גרם למיקום/רוחב לא מדויקים במחשב. תוקן למדוד את תיבת הקלט ישירות.
- דף "הוצאות": צ׳קים מפוצלים (מחולקים בין כמה מחלקות) הוצגו כ"בהמתנה לסיווג" — תוקן להציג "מפוצל" כשיש הקצאות בפועל.
- דף "דוחות מחלקות" (ledger): הוחלף הבורר "בחר מחלקה + הצג דוח" בכפתורי מחלקה לחיצים ישירות.
- דוח מחלקה: הכנסות/הוצאות שסומנו כ"ישנות" (לא נכללות במאזן) הוצגו כלל לא ברשימה — עכשיו מוצגות עם תג "ישן — לא נכלל במאזן" (עדיין לא נכללות בסכומים). נוספה עמודת "נפרע?" לצ׳קים/העברות, וכפתור להחלפת הסימון ישן/לא ישן בכל שורה (למנהל כספים).

## 2026-08-19 (המשך 11)

### תיקון תקלת תצוגה נוספת במובייל + שיפורי חלוניות חיפוש
- בחלון "פרטים" של צ׳ק/פריסה ובחלון "כל ההוצאות עבור [ספק]": הטבלאות לא היו עטופות בקונטיינר עם גלילה אופקית, כך שבמסך צר (מובייל) הן פשוט נחתכות בלי אפשרות לגלול ולראות את שאר העמודות — נראה כאילו התצוגה "שבורה". תוקן ע״י עטיפת שתי הטבלאות (ופירוט הפריסה לפי מחלקה) ב-`overflow-x-auto`, כמו כל טבלה אחרת באתר.
- רכיב "בחירה חכמה" (SearchableSelect) — תיבת החיפוש המשמשת לבחירת מחלקה/חשבון בנק/ספק בטפסים — סבל מאותה בעיה שתוקנה קודם בתפריטי הסינון של הטבלאות: הרשימה הנפתחת הייתה ממוקמת יחסית לתיבה (`absolute`), ונחתכת/מתעוותת כשהיא בתוך חלון קופץ או טבלה עם גלילה. תוקן באותה שיטה — הרשימה נעוצה עכשיו למיקום האמיתי על המסך (`fixed`), נפתחת נקי מתחת לתיבה בלי לפגוע בשאר העמוד.
- תיבת החיפוש (SearchableSelect) וגם תפריטי הסינון של הטבלאות מציגים עכשיו סיכום "X מתוך Y תוצאות/שורות" ברגע שיש חיפוש/סינון פעיל.

## 2026-08-19 (המשך 10)

### מיון וסינון בכל הטבלאות — סיום המעבר השיטתי
- דף "כל התנועות": הטבלה המאוחדת קיבלה מיון וסינון בעמודות (בנוסף לסינון השרתי הקיים בטופס למעלה — מסנן/למיין מהיר על התוצאה המסוננת).
- דף הבית (דשבורד): טבלת "מאזן בנקים", טבלת "התחשבנות פנימית בין מחלקות" וטבלת "רישומים ידניים ממתינים לאישור" קיבלו מיון וסינון.
- דף הגדרות: טבלת "חשבונות בנק" (משתפת רכיב עם דף הבית) וטבלת "משתמשים והרשאות" קיבלו מיון וסינון.
- חלון "פרטים" של צ׳ק/פריסה: טבלת פירוט הצ׳קים בפריסה קיבלה מיון וסינון. חלון "כל ההוצאות עבור [ספק]" קיבל מיון וסינון גם הוא.
- תחזית חשבון בנק: טבלת "הכנסות צפויות ממתינות" וטבלת הארכיון קיבלו מיון וסינון. טבלת "פירוט מלא לפי פריט" בסימולציה קיבלה מיון וסינון (יתרת הסימולציה ממשיכה להתייחס לכל שורה לפי הסדר המקורי, גם כשממיינים תצוגה).

בזה הושלם המעבר השיטתי שהתבקש על פני כל הטבלאות באתר — לכל טבלת "רשימה/עיון" יש כעת אופציה למיון וסינון בכל עמודה.

## 2026-08-19 (המשך 9)

### מיון וסינון בכל הטבלאות — המשך
- טבלאות "קטגוריות ממתינות לשיוך" ו"כל הקטגוריות" בדף ניהול קטגוריות קיבלו מיון וסינון.
- טבלת ספקים בדף ניהול ספקים קיבלה מיון וסינון (שם, הערות, תאריך הוספה).
- דף הכנסות: טבלת "הוראות קבע — תשלומים שנותרו" וטבלת ההכנסות הראשית קיבלו מיון וסינון מלאים (תאריך, קטגוריה, תורם, סכום, מקור, מחלקות, התחשבנות, קבלה, מס׳ הוראה).

## 2026-08-19 (המשך 8)

### תפריט וניהול הוראות קבע
- סדר לשוניות התפריט העליון: דשבורד ← צ׳קים והעברות ← הכנסות ← כל התנועות ← (הוצאות, דוחות מחלקות, תחזית, הגדרות).
- טבלת הוראות הקבע עברה מחלון קופץ להיות מוצגת ישירות בעמוד "צ׳קים והעברות", ממש מעל מקטע "צ׳קים להנפקה" — כולל מיון וסינון. הוספת הוראה חדשה נשארה בחלון קופץ (כפתור "+ הוראת קבע חדשה" למעלה).
- כפתור "הכנסה / הוצאה" ידני זמין עכשיו גם מעמוד "צ׳קים והעברות", לא רק מ"דוחות מחלקות".
- הוסר גם מהוראת קבע השדה "כמה ימים מראש אפשר לאשר" — אותו כלל אחיד וקבוע של 3 ימים שכבר חל על הכנסות צפויות חל עכשיו גם על הוראות קבע, בלי צורך להגדיר את זה בנפרד לכל הוראה.

### מיון וסינון בכל הטבלאות — המשך
- טבלת ניהול המחלקות קיבלה מיון וסינון (שם, קוד, חשבון בית, שימוש), בהתאם לכלל שנקבע.

## 2026-08-19 (המשך 7)

### פרטי צ'ק — הפרדה בין פריסה בודדת לסיכום פריסה
- בחלון "פרטים" של צ'ק: כשמדובר בפריסה, מוצג עכשיו סיכום נפרד "לפי מחלקה — כל הפריסה" עם הסכום הכולל שכל מחלקה מקבלת על פני כל הפריסה, בנוסף לטבלת הפירוט לפי צ'ק בודד. בצ'ק בודד (לא פריסה) הכל נשאר כמו קודם.

### דוחות מחלקות — הכנסה/הוצאה ידנית עם בחירת מחלקה חופשית
- כפתור "הכנסה / הוצאה" בדף "דוחות מחלקות" זמין תמיד למעלה (לא רק אחרי פתיחת מחלקה ספציפית), ומאפשר לבחור לאיזו מחלקה להזין — לא רק את המחלקה שפתוחה כרגע.

### תיקון תקלת תצוגה בסינון טבלאות במובייל
- תפריט הסינון שנפתח מכותרת עמודה בטבלה (המשולש/משפך) גרם לטקסט חופף ולא קריא במסכים צרים, כי הקונטיינר של הטבלה (עם גלילה אופקית) חתך את הפריסה שלו. תוקן ברמת הרכיב המשותף לכל הטבלאות — התפריט נעוץ עכשיו למיקום האמיתי על המסך, לא חתוך יותר. משפיע על כל טבלה באתר עם סינון/מיון.
- בעקבות זה: גם טבלת "סיכום יומי" בתחזית חשבון בנק קיבלה אופציה למיון וסינון, כמו שאר טבלאות המערכת. **כלל ברזל למשך העבודה מכאן ואילך: כל טבלה חדשה שנבנית באתר תכלול מיון וסינון** — עדיין לא עברתי טבלה-טבלה על כל הטבלאות הישנות שטרם קיבלו את זה, אבל זו התבנית שאעבוד לפיה קדימה.

### דף "ניהול צ'קים והעברות" — ניקוי כפילויות + בית להוראות קבע
- הוסרו 3 המקטעים שהיו כפולים לדף "הוצאות": "כל הצ'קים וההעברות", "צ'קים הדורשים סיווג מחלקה" ו"צ'קים שהונפקו". כל אלה כבר זמינים בצורה מסודרת בדף ההוצאות (כולל שיוך מחלקה בכמות לצ'קים לא מסווגים).
- ניהול הוראות קבע (הוספה ועריכה של קיימות) עבר להיות כאן, בכפתור בראש הדף — הוסר לגמרי מדף ההוצאות ומדף ההגדרות (שם נשאר רק קישור מפנה).
- בדף הבית: כפתור "הוראת קבע חדשה" מפנה עכשיו לכאן במקום להגדרות, ורישום הכנסה/הוצאה ידנית הפך לכפתור נפרד (היה טופס קבוע על המסך).

### תחזית מחלקה — שורת "ממתין לאישור מנהל"
- בתחזית מחלקתית מוצגת עכשיו, כשרלוונטי, כרטיסיית אזהרה נפרדת עם סך ההוצאות וההכנסות (צ'קים/העברות וגם רישומים ידניים) שעדיין ממתינים לאישור מנהל כספים — כדי שיהיה ברור שיש עוד תנועות "בדרך" שלא נכללות בחישוב התחזית למטה (שממשיך לכלול רק דברים שכבר אושרו).

## 2026-08-19 (המשך 6)

### הכנסות צפויות — ארכיון + חלון אישור קבוע (3 ימים)
- הכנסה צפויה שסומנה כ"התקבל" או כ"לא התקבל" כבר לא מוצגת ברשימה הראשית — היא עוברת ל"ארכיון" (כפתור חדש עם מונה, פותח חלון עם ההיסטוריה, כולל אפשרות מחיקה).
- חלון האישור (מתי אפשר לסמן שהכנסה צפויה התקבלה) קבוע עכשיו על 3 ימים לפני התאריך הנקוב, אחיד לכל ההכנסות הצפויות — הוסר השדה שדרש להגדיר את זה ידנית לכל הכנסה בנפרד ביצירה/עריכה.

## 2026-08-19 (המשך 5)

### תחזית בנק — שכלולים לסימולציה ואישור הכנסה
- "צפי יתרה לתאריך מסוים" משתתף עכשיו באותה סימולציה כמו הטבלה למטה — אם מסמנים שורות כ״לא יוצא״, החלונית הזו מציגה גם את היתרה בסימולציה לתאריך שנבחר, לצד היתרה האמיתית.
- עמודת הסימון "לא יוצא (סימולציה)" בטבלת "פירוט מלא לפי פריט" הועברה להיות העמודה האחרונה בטבלה.
- סימון הכנסה צפויה כ"התקבל" שואל עכשיו במפורש "האם לעדכן בהתאם את יתרת חשבון הבנק?" — רק אם עונים כן, הסכום מתווסף ליתרה; אחרת הסטטוס מתעדכן בלי לגעת ביתרה. זו הדרך היחידה שהכנסה יכולה בכל זאת לעדכן יתרה, אחרי שהעדכון האוטומטי הוסר.

## 2026-08-19 (המשך 4)

### הכנסה כבר לא מעדכנת אוטומטית את יתרת הבנק
- רישום הכנסה חדשה הפסיק לעדכן ישירות את יתרת חשבון הבנק. עכשיו כל סוגי התנועות מתנהגים אחיד — צ׳קים, העברות ורישומים ידניים גם ככה לא נגעו ביתרה — ורק כפתור "עדכון יתרה" בתחזית משנה אותה. מטרת השינוי: למנוע סטייה שקטה בין היתרה שהמערכת חישבה לבין היתרה שאושרה ידנית מול הבנק.

### הכנסה צפויה בתחזית — חלון נפרד + עריכה
- "הוספת הכנסה צפויה" הפכה לכפתור שפותח חלון נפרד, במקום טופס קבוע בתוך הדף.
- לכל שורה ברשימת ההכנסות הצפויות נוסף כפתור "עריכה" (סכום, תאריך, תיאור, חשבון בנק, ימים מראש) — לצד האישור/מחיקה שכבר היו.

### תחזית בנק — סימולציית "מה אם" זמנית
- בטבלת "פירוט מלא לפי פריט" בתחתית תחזית הבנק אפשר עכשיו לסמן שורות כ״לא יוצא״ (למשל צ׳ק שהוחלט לא להפקיד) — זו סימולציה בלבד, לא משנה שום נתון אמיתי במערכת.
- כשמסמנים שורות, מופיעה בסיכום החודשי למעלה שורת "הוצאות מושהות (סימולציה)" ו"יתרת סגירה בסימולציה" בנוסף לנתונים האמיתיים, כדי שיהיה ברור שזו הגדרה זמנית ולא תחזית אמיתית.
- אפשר לשמור את הסימולציה (נשמרת בדפדפן בלבד, לפי חשבון) כדי שתישאר גם אחרי רענון הדף, ולנקות אותה בלחיצה.

## 2026-08-19 (המשך 3)

### הוראות קבע — הוצאות בלבד + "כמה ימים מראש זה כבר קורה"
- הוראת קבע היא עכשיו תמיד הוצאה — הבורר "כיוון" (הכנסה/הוצאה) הוסר מטופס היצירה ומטבלאות הרשימה (בהגדרות ובדף הוצאות); כל הכנסה עתידית מנוהלת דרך "הכנסות עתידיות צפויות" בתחזית בנק בלבד.
- גם בהוראת קבע וגם בהכנסה צפויה אפשר עכשיו לציין "כמה ימים לפני התאריך זה בדרך כלל כבר קורה/מגיע" — שדה אופציונלי (0 כברירת מחדל). כשמוגדר, האישור/ההתראה על התנועה נפתחים כבר מהיום ההוא, ולא רק מהתאריך הנקוב עצמו — כדי לשקף מתי זה באמת יורד/נכנס בפועל, לא רק את התאריך ה"רשמי".
- אגב התיקון: תיבת ה"טרם סומן" בהכנסות צפויות הייתה נפתחת רק החל מהיום שאחרי התאריך הנקוב (לא מהתאריך עצמו) — תוקן יחד עם השינוי הזה.

## 2026-08-19 (המשך 2)

### תחזית — הכנסה צפויה: חובת חשבון בנק + אפשרות להישנות חודשית
- בטופס "הכנסות עתידיות צפויות" בתחזית בנק חייבים עכשיו לבחור באופן מפורש לאיזה חשבון בנק ההכנסה שייכת (בורר עצמאי בטופס עצמו, לא רק לפי הסינון הנוכחי בעמוד) — כך אפשר גם להוסיף הכנסה צפויה לחשבון אחר מזה שמוצג כרגע.
- נוספה אפשרות לסמן שהכנסה צפויה "חוזרת כל חודש" ולא רק חד-פעמית: מציינים לכמה חודשים, והמערכת יוצרת שורה נפרדת וניתנת לאישור/מחיקה עצמאית לכל חודש (לא הוראת קבע אמיתית — אין לזה מחלקה/קטגוריה, וזה לא משפיע על ההתחשבנות הפנימית, רק על תחזית הבנק).

## 2026-08-19 (המשך)

### ניהול מחלקות — היפוך תגית הבעלות על חשבון
- הפוך למה שהיה: מחלקה שלא מנהלת את החשבון מציגה רק את שם הבנק ומספר החשבון (בלי תגית נוספת). המחלקה הראשית שבאמת מנהלת את החשבון היא היחידה שמסומנת — בהדגשה ובתגית "מנהלת את החשבון".

## 2026-08-19

### תיקון קריטי: שאילתת חשבונות בנק נשברה בכל האתר
- מאז שנוסף שדה `home_bank_account_id` למחלקות (חשבון בית), כל שאילתה שמביאה חשבונות בנק יחד עם שם המחלקה שלהם (`departments(name)`) נהייתה דו-משמעית ברמת ה-DB — יש עכשיו שני קשרים אפשריים בין הטבלאות (חשבון בית של מחלקה, ובעלות על חשבון), ו-Supabase לא ידע איזה מהם להשתמש. זה גרם לשאילתה כולה להיכשל בשקט ולהחזיר רשימת חשבונות ריקה.
- זו הסיבה האמיתית לכך שחשבונות הבנק "נעלמו" בניהול מחלקות (ובעוד מקומות: דשבורד, צ׳קים, הוצאות, תחזית תזרים, הכנסה חדשה, הגדרות, כפתורי פעולה מהירה) — לא תקלת תצוגה כמו שחשבתי קודם.
- תוקן בכל 7 המקומות שנפגעו: השאילתה מציינת עכשיו במפורש לפי איזה קשר להביא את שם המחלקה (`departments!bank_accounts_department_id_fkey`), מה שמסיר את הדו-משמעות. הנתונים עצמם מעולם לא נפגעו — זו הייתה אך ורק תקלת שליפה.

## 2026-08-18 (המשך 10)

### הוראות קבע — הגבלת זמן, הפעלה אוטומטית, וניהול מדף הוצאות
- יצירת הוראת קבע חדשה מאפשרת עכשיו להגביל אותה למספר חודשים (מחשב תאריך סיום), או להשאיר "קבוע ללא הגבלת זמן" (ברירת המחדל). מכבד את זה גם בתחזית וגם באישור סכומים.
- הוראת קבע עם תאריך **וגם** סכום ידועים מראש (תאריך קבוע + סכום קבוע) כבר לא דורשת אישור ידני: ברגע שהתאריך שלה מגיע, המערכת יוצרת עבורה אוטומטית רישום הוצאה/הכנסה מאושר — הוא נכנס ישר לרצף דוח ההוצאות (עם תווית "הוראת קבע"), בדיוק כמו כל תנועה אחרת.
- דף "הוצאות" קיבל כפתור "הוראות קבע" חדש — פותח חלון לניהול (הפעלה/השבתה, מחיקה) והוספה של הוראות קבע, בלי לצאת לדף ההגדרות.
- "אישור סכומים בפועל" (להוראות עם סכום/תאריך משוער) עבר מדף ההגדרות לדף "ניהול צ׳קים והעברות", צמוד לרשימת הצ׳קים/העברות שלא נפרעו — באותו מקום שבו מאשרים תנועות אחרות שטרם ירדו בפועל.

## 2026-08-18 (המשך 9)

### ניהול מחלקות — תיקון בחירת חשבון בית
- בחירת "חשבון בית" (בעריכת מחלקה ובהוספת מחלקה חדשה) הוחלפה מ-select רגיל לרכיב בחירה מוקלד — אותו תיקון שכבר בוצע בתחזית תזרים. הבעיה שדווחה (רשימת החשבונות "נעלמת"/לא ניתנת לבחירה) היא כנראה תקלת עיצוב של הבורר הטבעי של הדפדפן; הנתונים עצמם (3 חשבונות הבנק הקיימים) נבדקו ולא נפגעו.

## 2026-08-18 (המשך 8)

### מטריצת יתרות נטו — סימון חובות לחשבון המרכזי + הסרת תנועות פתוחות
- שורה במטריצת היתרות שבה הזכאי הוא המחלקה המרכזית (זו שרוב המחלקות משתמשות בחשבון שלה כחשבון בית) מסומנת עכשיו באדום — כדי שיהיה ברור מיד שזה חוב כלפינו.
- הטבלה "תנועות פתוחות (לפני נטו)" מתחת למטריצה הוסרה לגמרי — המטריצה הנטו מספיקה.

## 2026-08-18 (המשך 7)

### שינוי מיתוג — "דשבורד מרכז חב״ד עפולה"
- שם המערכת עודכן מ"קופה·מוסד" ל"דשבורד מרכז חב״ד עפולה" — בסרגל הניווט, בדף ההתחברות, בכותרת הדשבורד הראשי, ובכותרת הטאב בדפדפן.

### תחזית תזרים — איחוד מסנן ותיקון בחירת חשבון
- שני הריבועים הנפרדים (בחירת חשבון/מחלקה + טווח ימים, ובנפרד "עד חודש" בטבלת הסיכום היומי) אוחדו לריבוע מסנן אחד בראש הדף. שדה "טווח ימים" הוסר לגמרי — "עד חודש" קובע לבד גם עד איפה מחשבים.
- בחירת חשבון בנק/מחלקה הפכה מ-select רגיל לרכיב בחירה מוקלד (כמו בשאר המערכת) שמחיל את הבחירה מיד — בלי כפתור "עדכן תחזית" נפרד, ובלי הבעיה שדווחה של קושי ללחוץ ולבחור מהרשימה.

### דוח מחלקה — כפתור הוספת תנועה בראש הדף
- "רישום ידני של הכנסה/הוצאה" בדוח מחלקה (`/ledger`) הפך מריבוע קבוע מתחת לדוח לכפתור "+ הכנסה / הוצאה" בראש הדף, ליד שם הדוח — לוחצים ונפתח חלון עם הטופס.

### סימון פריסות — גם בדוח הוצאות וגם בדוח לספק
- תגית "פריסה · סה״כ X ₪" (שנוספה קודם לדוח מחלקה) נוספה גם לדף "הוצאות" וגם לחלון "כל ההוצאות עבור [ספק]" — בכל מקום שמוצגת פריסה, מוצג גם הסכום הכולל שלה.

## 2026-08-18 (המשך 6)

### ניהול מחלקות — עידון תצוגת הבעלות על חשבון
- עמודת "שימוש" כבר לא מציגה "X חשבונות" (מיותר — הבעלות כבר מסומנת בעמודת "חשבון בית"), נשארה רק "X קטגוריות".
- מחלקה שבאמת מנהלת את חשבון הבית שלה: שם הבנק ומספר החשבון עצמם מודגשים (מודגש + צבע ראשי) במקום תגית טקסט נפרדת — כך ההדגשה על החשבון עצמו, לא לידו.

## 2026-08-18 (המשך 5)

### דוח מחלקה — עמודת "סוג" מפורטת יותר
- עמודת "סוג" בדוח מחלקה כוללת כעת גם את כיוון התנועה וגם את אופן התשלום: "הכנסה — תרומות (אשראי)", "הוצאה — צ׳ק", "הוצאה — העברה", "הכנסה — קבוע (אושר)" / "הוצאה — קבוע (אושר)" לרישום שנוצר מאישור הוראת קבע, ו"הכנסה — ידני" / "הוצאה — ידני" לרישום ידני רגיל.

## 2026-08-18 (המשך 4)

### דוח מחלקה — סימון פריסות בין תנועות
- בדוח מחלקה, כל צ׳ק/העברה ששייכים לאותה פריסה (למשל תשלומים חודשיים קבועים לאותו ספק) מסומנים כעת בתגית "פריסה · סה״כ X ₪" ליד התיאור, כדי שיהיה ברור מיד שכמה שורות שייכות לאותה התחייבות כוללת ומה הסכום הכולל שלה.

## 2026-08-18 (המשך 3)

### הוראות קבע — אישור סכום בפועל להוראות עם סכום/תאריך משוער
- כל הוראת קבע עם סכום משוער (תאריך קבוע או תאריך לא קבוע) שהתאריך הצפוי שלה כבר עבר, ושעדיין לא אושרה, מופיעה כעת בהגדרות מערכת תחת "אישור סכומים בפועל" — לפני טבלת הוראות הקבע.
- באישור: מזינים תאריך וסכום בפועל, ואפשר לבחור למחלקה שאליה זה שייך (לאו דווקא זו שהוגדרה בהוראה עצמה) או לפצל את הסכום בין כמה מחלקות — בדיוק כמו פיצול הכנסה. האישור נרשם כרישום ידני מאושר רגיל (עם כל הזיהוי האוטומטי של העברות בין מחלקות), כך שהוא מופיע בכל מקום שבו רישום ידני מופיע (דוח מחלקה, דף הוצאות וכו׳).
- כל תקופה של הוראת קבע ניתנת לאישור פעם אחת בלבד לכל מחלקה — ניסיון לאשר שוב את אותה תקופה נחסם.

## 2026-08-18 (המשך 2)

### הוראות קבע — פיצול בין מחלקות + טופס שמותאם לתדירות
- אפשר עכשיו לסמן הוראת קבע כ"מפוצלת" בין כמה מחלקות עם סכום קבוע לכל מחלקה (בדיוק כמו פיצול צ׳ק בין מחלקות) — נשמר בטבלה חדשה `recurring_schedule_allocations`, ומופיע נכון גם בתחזית התזרים המחלקתית של כל מחלקה מעורבת (עם החלק היחסי שלה בלבד).
- טופס הוספת הוראת קבע (בהגדרות מערכת) הפך לרכיב אינטראקטיבי: מציג רק את שדה התאריך הרלוונטי לתדירות שנבחרה (יום בחודש / יום בשבוע / תאריך חד-פעמי) במקום את כל השלושה יחד, ומאפשר להחליף בין "מחלקה אחת" ל"פיצול בין מחלקות" עם עורך הקצאות (כמו בהכנסה מפוצלת).
- בטבלת הוראות הקבע, הוראה מפוצלת מוצגת כ"מפוצל (X מחלקות)" עם פירוט הסכום לכל מחלקה ב-tooltip.

## 2026-08-18 (המשך)

### ניהול מחלקות — סימון בעלות על חשבון בית
- ברשימת המחלקות, כל מחלקה שבאמת מנהלת חשבון בנק (יש חשבון ששייך אליה) מסומנת בתגית "מנהלת חשבון" ליד חשבון הבית שלה — לא רק המחלקה המרכזית, כל מחלקה עם חשבון פרטי (למשל בית הספר, בית הכנסת הפתוח).
- מחלקה שחשבון הבית שלה שייך בפועל למחלקה אחרת (כלומר היא לא הבעלים, ומשתמשת בחשבון המרכזי או בחשבון של מחלקה אחרת) מסומנת בתגית "שייך ל-X" שמבהירה למי החשבון שייך.

### דוחות מחלקות — ניקוי סופי
- כפתור "קיזוז" בדף ההתחשבנות הפנימית הוסר לגמרי (הפעולה כבר לא נדרשת — היתרות מחושבות אוטומטית מהתנועות עצמן, אין "סגירת חוב" ידנית נפרדת).
- כשבוחרים מחלקה ספציפית בדף "דוחות מחלקות", מטריצת היתרות ורשימת התנועות הפתוחות בין-מחלקתיות מוסתרות (הן רלוונטיות רק לתצוגה הכללית של כל המחלקות יחד).
- לכל שורה ברשימת התנועות המעורבת של דוח מחלקה נוסף עמודת "סוג" (שם הקטגוריה בהכנסה, "העברה"/"צ׳ק" בהוצאה, "הכנסה ידנית"/"הוצאה ידנית" ברישום ידני) — כדי להבין מייד איך הסכום הכולל מורכב.

## 2026-08-18

### אותו מנגנון אוטומטי גם על צ'קים/העברות (כולל פיצולים)
- ההזדהות האוטומטית בין מחלקה לחשבון בית שנוספה לרישום ידני חלה כעת גם על צ'קים והעברות: כל צ'ק/העברה מאושרים (כולל כל שורה בצ'ק מפוצל בין כמה מחלקות) שהמחלקה שלהם שונה מהמחלקה של חשבון הבנק בפועל — יוצרים אוטומטית רישום חוב ב"התחשבנות הפנימית", בדיוק לפי אותו עיקרון של הכנסות ורישומים ידניים.
- הופעל רטרואקטיבית לפי אישור מפורש: 83 צ'קים/פיצולים מאושרים שכבר קיימים (כ-118,778 ₪ סה״כ) קיבלו עכשיו רישום חוב מתאים, נבדק ואומת מול הנתונים בפועל.
- תוקן באג: אם היה נמחק רישום ידני עם רישום חוב פתוח, המחיקה הייתה נכשלת (חסרה מגבלת "מחיקה בשרשור"). תוקן עכשיו, ואותו דבר הוגדר נכון מראש עבור צ'קים.

### חשבון בית לכל מחלקה — ביטול הצורך ב"צד ג'" ידני
- לכל מחלקה יש כעת "חשבון בית" מוגדר במפורש (בניהול מחלקות): החשבון הפרטי שלה אם יש, אחרת חשבון "פעילות בית חב״ד" המרכזי. שדה חדש חובה, נקבע אוטומטית להיסטוריה הקיימת.
- ברישום ידני של הכנסה/הוצאה: שדה "צד ג'" הוסר לגמרי. חשבון הבנק מתמלא אוטומטית לפי חשבון הבית של המחלקה שנבחרה, וניתן לשנות אותו — אם נבחר חשבון ששייך למחלקה אחרת, המערכת יוצרת אוטומטית רישום חוב ב"התחשבנות הפנימית" בין שתי המחלקות (בדיוק כמו שהכנסות כבר עובדות היום עם "מחלקה בעלים" מול "מחלקה מנפיקת"), ללא צורך לבחור צד ג' ידנית.
- דוגמה: הוצאה של "בית הספר" (יש לו חשבון פרטי) שנרשמת עם חשבון הבנק של "פעילות בית חב״ד" (כי בפועל ההעברה עברה למרכזי) — נוצר אוטומטית רישום שבית הספר חייב לפעילות בית חב״ד, בלי שום שדה נוסף.
- יצירת חשבון בנק חדש למחלקה מעדכנת אוטומטית את חשבון הבית שלה לחשבון החדש.
- הוסרו לגמרי: שדה "צד ג'", מנגנון הרשומה המקושרת (RPC ייעודי), וה-badge "העברה" הישן — הכל מוחלף בזיהוי אוטומטי לפי חשבון בנק.

### דף "דוחות מחלקות" (לשעבר "התחשבנות פנימית") — עיצוב מחדש לפשטות
- בראש הדף בורר מחלקה — בחירה מציגה מיד, בתוך הדף עצמו, דוח מחלקתי מינימלי: 3 מספרים בלבד (סה״כ הכנסות, סה״כ הוצאות, וכמה המחלקה חייבת/זכאית לחשבון שדרכו היא פועלת), ומתחת רשימה אחת מעורבת של כל התנועות (הכנסות והוצאות יחד, מהחדש לישן) — בלי טבלאות נפרדות, בלי פילטר טווח, בלי צפי. וכן טופס "רישום ידני של הכנסה/הוצאה" ממוקד לאותה מחלקה, ישר מתחת.
- אותו רכיב פשוט (`department-report.tsx`) משמש גם את הדוח המחלקתי הנפרד (`/reports/[departmentId]`, שנשאר זמין להדפסה), כדי שלא יהיו שתי גרסאות שונות של אותו דוח.
- מטריצת ההתחשבנות הפנימית בין מחלקות נשארה בדף (למטה), ללא שינוי.
- כל הטבלאות בדף (רשימת התנועות המעורבת בדוח המחלקתי, מטריצת היתרות, ותנועות ההתחשבנות הפתוחות) קיבלו מיון וסינון בלחיצה על כותרת העמודה, כמו בשאר המערכת.
- ברישום ידני שהחשבון שנבחר שייך למחלקה אחרת (זוהה אוטומטית, ראו למעלה), רשימת התנועות מציגה "העברה מ-X" / "העברה ל-X" במקום "רישום ידני" גנרי — כדי שיהיה ברור מאיפה כל סכום בא.

### בדיקת אבטחה מקיפה
- נמצא ותוקן: טבלת `print_queue` (שייכת לפרויקט Supabase אחר לגמרי — כנראה חנות יודאיקה, לא קשורה למערכת הזו) הייתה חשופה לקריאה וכתיבה ללא כל אימות (`anon`), כולל שם/טלפון/כתובת של לקוחות אמיתיים. ננעלה לגמרי (הוסרו מדיניות ה-RLS הפתוחות) — עד שתחובר גישה מסודרת (authenticated/service role) מהאפליקציה הייעודית שלה.
- שופר: ברישום ידני עם "צד ג'" (העברה בין מחלקות), שורת האישור מציגה כעת גם את שם מי שהגיש את הבקשה — כך שמנהל כספים שמאשר בקשה במחלקה שלו יודע אם היא הוגשה על ידי מישהו ממחלקה אחרת.
- נבדקו ואומתו כתקינים: כל מדיניות ה-RLS, כל 11 ה-Views (כולם עם security_invoker=true), כל פונקציות ה-SECURITY DEFINER (כולל הרשאות anon/authenticated בפועל), ה-Edge Function ליצירת משתמשים, ואין שימוש ב-dangerouslySetInnerHTML או SQL גולמי בקוד.
- חוזק: `settleBalances` (התחשבנות פנימית בין מחלקות) מקבל כעת בדיקת `requireFinanceAdmin()` מפורשת לפני קריאת ה-RPC, כשכבת הגנה נוספת מעבר ל-RLS (שכבר חסם את הפעולה בפועל).

### הוראות קבע — 3 סוגים
- שדה "סוג" בהוראת קבע הפך משדה לא-פעיל (לא השפיע בפועל על התחזית) לשדה אמיתי עם 3 אפשרויות: תאריך קבוע עם סכום קבוע, תאריך קבוע עם סכום משוער, ותאריך לא קבוע עם סכום משוער (לתדירות חודשית בלבד — השדה "יום בחודש" נשאר ריק). הוראות עם סכום/תאריך משוער מסומנות בתגית "משוער" בטבלה, ובתחזית עצמה.
- תוקן באג קיים: הוראת קבע חודשית עם "יום בחודש" שאינו 1 (למשל שכירות ב-10 לחודש) מעולם לא הופיעה בתחזית התזרים בפועל — כעת מוצגת בתאריך הנכון בכל חודש, גם לפי חשבון בנק וגם לפי מחלקה.

### רישום ידני של הכנסה/הוצאה — צד ג' והעברות בין מחלקות
- שדה מחלקה ברישום ידני כבר לא נבחר אוטומטית — חייבים לבחור במפורש (כמו בשאר הטפסים בהנפקה).
- שדה חדש ואופציונלי "חשבון בנק" — לתיעוד דרך איזה חשבון בפועל עבר הכסף.
- שדה חדש ואופציונלי "צד ג' / מחלקה מקבלת" — מאפשר לרשום העברה אמיתית בין שתי מחלקות: לדוגמה הוצאה שנזקפת למחלקת "בית ספר" אך בפועל מיועדת ל"לב חב״ד", דרך חשבון הבנק של "פעילות מרכזי". במקרה כזה נוצרת אוטומטית גם רשומה תואמת (הפוכה בכיוון) אצל המחלקה השנייה, ושתי הרשומות מאושרות/נדחות תמיד יחד. מסומן בתגית "העברה" בטבלת האישורים.

### חלונות (Modal)
- כתיבה מחדש של רכיב החלון הבסיסי (Modal) להשתמש באלמנט `<dialog>` הטבעי של הדפדפן במקום backdrop מותאם אישית — פותר סופית את התקלות שחזרו שוב ושוב בלחיצה על "פרטים"/שם ספק ובחלון הנפקת הצ'קים ברצף. נבדק בפועל מול דפדפן אמיתי (Playwright) לפני שהוטמע בכל המערכת.

### מיון וסינון בטבלאות (כמו אקסל)
- כל כותרת עמודה בכל טבלאות עמוד "צ'קים והעברות" (כל הצ'קים, צ'קים להנפקה, ממתינים לאישור, שהונפקו, פיגורים בצ'קים/העברות) הפכה למיון בלחיצה + תפריט סינון בסימן משפך, בדיוק כמו באקסל.
- טבלת "צ'קים להנפקה" מציגה כעת הפרדה ברורה וצבעונית בין חשבונות הבנק שמהם מנפיקים, עם כותרת קיבוץ לכל בנק (כמו שכבר היה בטבלאות הפיגורים).

### שיוך מחלקה אוטומטי לפי חשבון בנק
- כל חשבון בנק משויך למחלקת "בית" קבועה. מעכשיו, צ'ק/הוצאה בלי שיוך מחלקה מפורש (וללא פיצול) מחושב במאזנים ובדוחות המחלקתיים תחת מחלקת הבית של חשבון הבנק שממנו הוא יצא — גם רטרואקטיבית על רשומות ישנות. תזכורת "ממתין לסיווג" בעמוד הצ'קים נשארת בדיוק כפי שהייתה, ללא שינוי.
- בעקבות זאת, בטופס "דרישת תשלום חדשה" מחלקה כבר לא שדה חובה (חשבון בנק כן נשאר חובה) — ניתן להשלים שיוך מחלקה מפורש בכל שלב, כולל בעריכה.

### חלונית "כל ההוצאות עבור [ספק]"
- כל שורה מציגה עכשיו תגית "פריסה" אם הצ'ק חלק מפריסה, וכן כפתור "פרטים" לצפייה בפירוט המלא, וכפתור עריכה מלא (כולל פיצול מחלקות) — בדיוק כמו בשאר טבלאות הצ'קים.

### פעולות מהירות (כפתור +)
- תפריט ה-"+" כולל כעת גם קישור ישיר ל"מעבר לתחזית" וגם קישור ישיר ל"צ'קים והעברות שהגיע תאריכם" (קופץ ישר לחלק הרלוונטי בעמוד הצ'קים).
- כל הפעולות שבתפריט ה-"+" מופיעות כעת גם כלוח כפתורים נעים בדף הדשבורד המרכזי, לנוחות מנהלי כספים.

## 2026-08-17

### דרישות תשלום וצ'קים
- שינוי שם החלק ל"דרישות תשלום", עם חיפוש נפרד לכל חלק בעמוד הצ'קים.
- טאב "הוצאות" חדש — מרכז את כל ההוצאות שכבר אושרו (צ'קים, העברות, הוצאות ידניות) עם אפשרות בחירה מרובה, שיוך מחלקה מרוכז, קיבוץ לפריסה, חלוקה בין מחלקות מתוך סכום כולל, עריכה, מיון (תאריך/ספק/מחלקה/בנק), מסנן "רק ללא מחלקה" וטווח תאריכים.
- זרימת אישור מנהל מפורשת: הבחנה בין "ממתין לאישור" ל"ממתין להנפקה" — הזנה ע"י מנהל כספים מאושרת אוטומטית.
- טופס "דרישת תשלום חדשה" מאוחד ומעוצב מחדש: נפתח כחלון נפרד, מפריד בין "כמה וסכום" לבין "פריסה בפועל", נשאר פתוח אחרי שמירה כדי להזין עוד בקשה ברצף.
- כפתורי עריכה/מחיקה + קיבוץ אוטומטי לפי בנק בכל טבלאות הצ'קים.
- מיזוג צ'קים/העברות מסוגים שונים — כעת עם אזהרה ואפשרות המרה לפני המיזוג, במקום כישלון.
- פריסת תשלומים: חישוב אוטומטי עם עיגול (מציג כמה עוגל), ותאריך השורה הראשונה ממלא אוטומטית את שאר השורות (ניתן לתקן ידנית).
- תיקון באג: מיזוג+פיצול (וגם פריסה של צ'ק בודד עם מחלקה יחידה) איבדו את שיוך המחלקות.
- "הנפקה מהירה": מספור רציף אוטומטי לצ'קים עם תאריך שחסר להם מספר, כפתור "עד כאן הונפק" לעצירת הרצף, ואפשרות "דלג על מספר" לצ'ק פגום (נרשם כמבוטל כדי שהרצף יתאים לפנקס בפועל).
- עמודת "תאריך הזנה" בטבלת צ'קים להנפקה, והזנת תאריך פירעון ישירה בטבלה בלי לפתוח עריכה.
- לחיצה על צ'ק מציגה פירוט מלא כולל כל הפריסה ופיצול המחלקות; לחיצה על שם ספק מציגה את כל ההוצאות שלו.

### ייבוא והתאמת צ'קים
- ייבוא צ'קים הותאם לפורמט ההדבקה האמיתי (מס' צ'ק, שם, סכום, תאריך, מצב V/ריק/X) כולל זיהוי שורת כותרת.
- ייבוא והתאמת בנק נפתחים כחלון אמיתי במקום בתוך העמוד.
- התאמת צ'קים: הקלדת מספר צ'ק במקום חיפוש ברשימה, עם הצעות חכמות לצ'קים שעבר תאריכם.
- איחוד כפתור "צ'ק/העברה חדש" לתוך "דרישת תשלום חדשה".

### תחזית בנק
- פירוט יומי לפי סוג (צ'קים / העברות / הוראות קבע / הכנסות).
- ריבוע סיכום לכל חודש: יתרת פתיחה, הכנסות, הוצאות, יתרת סגירה.
- גישה ישירה מהדשבורד, אופק תחזית ארוך יותר, ריבועי חודשים מוערמים אחד מעל השני.
- בחירת "עד חודש" מצטברת בטבלה היומית, עם פס מפריד בין חודשים.
- ריבוע בדיקת יתרה לפי תאריך בודד.

### הוצאות/הכנסות ישנות
- אפשרות לתייג הוצאה או הכנסה כ"ישנה" (מוצגת בדוח המחלקה אך לא נכללת במאזן), עם אפשרות להחזיר אותה לחישוב.

### עיצוב
- עיצוב ויזואלי מחודש לכל האתר — פלטת צבעים חמה, טיפוגרפיה, אנימציות עדינות.
- תיקון רווחים ריקים גדולים בין עמודות בטבלאות בכל המערכת.
- צמצום נוסף של הריווח בין עמודות הטבלאות — טקסט ארוך (הערות, פירוט פיצול מחלקות) נשבר לכמה שורות במקום למתוח את העמודה.
- כל חלק בדף הצ'קים ניתן לקיפול — חץ קטן בקצה הכותרת שמכווץ/פותח את התוכן.
- הפרדה ויזואלית צבעונית בין חשבונות בנק שונים (נקודת צבע + פס בצד) בכל הטבלאות המקובצות לפי בנק, כולל "צ'קים והעברות שהגיע תאריכם ולא נפרעו".

### תיקוני באגים
- דף "הוצאות" הציג רשימה ריקה — שגיאה בשאילתה (עמודה לא קיימת בטבלת חשבונות בנק).
- תאריך בפריסת תשלומים "קפץ" יום אחורה בגלל בעיית אזור זמן.
- הקלדת תאריך גרמה לקפיצות תוך כדי ההקלדה עצמה.
- ייבוא/עריכת תאריך עם שנה דו-ספרתית (למשל 20/8/26) לא הומר נכון ל-2026.
- חלונות גדולים (הנפקה מהירה, כרטיס ספק) נתקעו באמצע ולא ניתן היה לגלול עד הסוף — הניסיון הראשון (הגבלת גובה + גלילה פנימית) לא באמת פתר את זה בפועל; טופל בסופו של דבר במעבר חזרה לשיטת גלילה פשוטה של כל החלון (כמו שהיה קודם), עם כפתור "אישור והנפקה" דביק (sticky) בהנפקה מהירה.
- תג "פריסה" הוצג רק בטבלת "כל הצ'קים" למרות שהיה אמור להופיע גם בצ'קים שהונפקו, בהעברות ובדף ההוצאות — תוקן להופיע בכל מקום.
- 5 צ'קים עם תאריך שגוי משנת "26" (במקום 2026), שנוצרו לפני שתיקון השנה הדו-ספרתית עלה, תוקנו ידנית ב-DB.

### הוצאות — סינון נוסף
- מסנן "רק ללא מחלקה" בדף ההוצאות.
- מסנן טווח תאריכים (מתאריך / עד תאריך) בדף ההוצאות.

### עוד תיקוני באגים ותוספות
- שינוי שם התג "חוץ מהמאזן הפנימי" ל"חישוב ישן" (עקבי עם הניסוח בשאר המערכת).
- עמודת "תאריך הנפקה" (מתי בפועל הוקצה מספר לצ'ק) נוספה לטבלת "צ'קים שהונפקו" — נפרדת מ"תאריך פירעון".
- תיקון "הנפקה מהירה": כפתור "דלג על מספר" דילג בטעות על המספר של השורה הבאה במקום השורה הנוכחית.

### הסרת חלונית מיותרת
- הוסרה חלונית "העברות שאושרו עם תאריך — ועוד לא שולמו" מדף הצ'קים — מיותרת, אישור ביצוע העברה זמין באותה מידה מתוך טבלת "כל הצ'קים וההעברות".

### תחזית — כולל פיגורים
- תיקון באג: צ'קים/העברות שהתאריך שלהם כבר עבר ועדיין לא נפרעו נעלמו לגמרי מהתחזית (בבנק ובמחלקה) במקום להשפיע עליה. עכשיו הם נכללים, מיוחסים להיום.
- בריבוע החודש הנוכחי בתחזית בנק נוספה שורה נפרדת "הוצאות ישנות שלא נפדו מחודשים קודמים", בנפרד מ"הוצאות" הרגילות.
- בטבלת הסיכום היומי נוספה עמודת "פיגורים (ישנים)".

### כותרות ותיקוני שם
- שינוי שם הטאב בתפריט העליון מ"צ'קים" ל"צ'קים והעברות".

### ביצועים
- תיקון: כל דף עשה בדיקת התחברות (auth) כפולה — פעם אחת ב-layout המשותף ופעם נוספת בכל דף (ולעיתים גם בפעולת שרת) — כל בדיקה כזו היא קריאת רשת נפרדת לשרת האימות של Supabase. עכשיו ה-בדיקה ממוזערת (cache) לכל בקשה כך שהיא רצה פעם אחת בלבד גם כשכמה מקומות קוראים לה, מה שאמור להאיץ משמעותית כל ניווט באתר.

### מניעת כפילות מספרי צ'קים
- הוספת אילוץ ברמת מסד הנתונים: לא ניתן יותר להזין את אותו מספר צ'ק פעמיים לאותו חשבון בנק (כולל צ'קים מבוטלים/מדולגים) — בכל דרכי ההזנה (יצירה, הנפקה, הנפקה מהירה, ייבוא הדבקה, עריכה). הודעת שגיאה ברורה במקום שגיאת מסד נתונים גולמית.

### הכנסות — מקור וסוג תשלום
- הדבקת הכנסות מזהה עכשיו גם עמודת "מקור" (אמצעי תשלום: חד פעמי / הוראת קבע / אשראי / העברה / אחר) ועמודת "סוג" — וכשהיא בפורמט "תשלום X מתוך Y" (למשל 1/10 או 4/12), המערכת שומרת את מספר התשלום הנוכחי ומספר התשלומים הכולל בנפרד, לשימוש בדוחות ובתחזית.
- שתי העמודות ניתנות לעריכה בתצוגה המקדימה של ההדבקה, ומוצגות גם ברשימת ההכנסות.
- כרטיס חדש "הוראות קבע — תשלומים שנותרו" בדף ההכנסות: לפי מספר הוראת קבע, מציג את התשלום האחרון שנקלט וכמה תשלומים נותרו (מחושב אוטומטית מעמודת "סוג").

### חלונות (Modal) — מעבר ל-<dialog> טבעי של הדפדפן
- אחרי כמה דיווחים חוזרים על תקיעות/בעיות בחלון פרטי צ'ק, כרטיס ספק וחלון ההנפקה ברצף (למרות תיקונים קודמים), הוחלף מנגנון החלון מ-div מותאם אישית ל-`<dialog>` הטבעי של הדפדפן — גלילה, לחיצה מחוץ לחלון לסגירה, ו-Escape מטופלים כולם באופן טבעי ועקבי בכל דפדפן/מכשיר, במקום מנגנון ידני. נבדק בפועל מול מנוע דפדפן אמיתי (פתיחה/סגירה/גלילה/לחיצה בחוץ) לפני שילוח.

### מיון וסינון בכותרות טבלה (בסגנון אקסל)
- בטבלת "כל הצ'קים וההעברות" (דף צ'קים) ובטבלת ההוצאות (דף הוצאות): לחיצה על כותרת עמודה ממיינת לפי אותה עמודה (עולה/יורד/ללא), וכל עמודה רלוונטית מקבלת גם תפריט סינון (רשימת תיבות סימון של הערכים הקיימים בעמודה), בדיוק כמו אקסל. שאר הטבלאות בדף הצ'קים (המקובצות לפי בנק) נשארות עם החיפוש הקיים בינתיים.

### יתרת בנק — תאריך עדכון
- ליד כל יתרת בנק (דשבורד, הגדרות, תחזית) מוצג עכשיו "עודכן לפני X ימים" / "עודכן היום" — היתרה נשארת הזנה ידנית בלבד כפי שהוחלט, אבל עכשיו ברור בכל מקום מתי עודכנה לאחרונה.

### תיקון באג — הכנסה צפויה שסומנה כהתקבלה נשארה בתחזית
- כשמסמנים הכנסה צפויה כ"התקבל", התחזית המשיכה לספור אותה קדימה כאילו עדיין לא הגיעה — כפל ספירה מול ההכנסה האמיתית שתירשם בנפרד. עכשיו רק הכנסות שעדיין "ממתין" (טרם סומנו) משפיעות על התחזית; הכנסה שעבר תאריכה וטרם סומנה נשארת בתחזית (מיוחסת להיום) עד שמסמנים, עם באנר התראה בולט בכרטיס "הכנסות עתידיות צפויות" שמראה כמה הכנסות ממתינות לאישור.

### כפתור פעולות מהירות (+)
- כפתור "+" צף בפינה הימנית-תחתונה, מוצג בכל עמוד (למנהלי כספים בלבד), שנפתח לתפריט קטן עם פעולות מהירות: "דרישת תשלום חדשה" ו"הכנסה צפויה חדשה" — פותח את אותם טפסים/פעולות שרת הקיימים בלי לנווט לעמוד אחר. בנוי להרחבה — עוד פעולות יתווספו בהמשך לאותה רשימה.

### פישוט "עזר להגדרת פריסה" בדרישת תשלום חדשה
- "עזר להגדרת פריסה" המורכב (מחלקות + סכום + מספר תשלומים + תאריך ראשון + מספר צ'ק ראשון) לא עבד טוב והוסר לגמרי. במקומו: שדה סכום כולל ומספר תשלומים + כפתור "פריסה" בלבד, שיוצר את מספר השורות עם חלוקה שווה של הסכום — תאריך/מספר צ'ק/מחלקה לכל שורה ממולאים בנפרד כמו בכל פריסה רגילה.

### מיון וסינון בכל טבלאות דף הצ'קים
- מיון וסינון בסגנון אקסל (לחיצה על כותרת עמודה) נוסף גם לטבלאות "צ'קים שהונפקו", "דרישות תשלום ממתינות לאישור", "צ'קים/העברות שהגיע תאריכם ולא נפרעו" ו"צ'קים להנפקה" — לא רק בטבלת "כל הצ'קים וההעברות" שכבר טופלה קודם.
- טבלת "צ'קים להנפקה" מקובצת עכשיו לפי חשבון בנק עם הפרדה צבעונית, כמו שאר הטבלאות בדף.
